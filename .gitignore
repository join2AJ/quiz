node_modules/
frontend/dist/
.env
*.log
.DS_Store
# Runtime data (Excel files, logo) — never commit participant results
backend/data/*
!backend/data/.gitkeep
!backend/data/sample-questions.json
# Question databases contain the answer key and staff passwords — never commit them.
*Quiz_Database*.json
*quiz_database*.json
*staff_roster*.json
